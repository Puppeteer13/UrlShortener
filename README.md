Run project:
  - adjust connection string in UrlShortener.Api project in appsettings.json
  - set both UrlShortener.Api and UrlShortener.WebUi as startup projects(or run them independently)
  - press start